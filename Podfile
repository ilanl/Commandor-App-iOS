platform :ios, '9.0'

source 'https://github.com/ilanl/pod-specs.git'

target 'App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Commandor'
  #pod 'VideoPlayerCommandor'
  		
  target 'AppTests' do
    inherit! :search_paths
    
  end

end
